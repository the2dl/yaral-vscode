const vscode = require('vscode');

function activate(context) {
    // Place future extension logic here. For now, we'll leave this empty.
}

function deactivate() { }

module.exports = { activate, deactivate }

